name: PlantPal CI Pipeline

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:13
        ports:
          - 5432:5432
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: plantpal_db
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:alpine
        ports:
          - 6379:6379

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.11

    - name: Install Python dependencies
      run: |
        python -m venv venv
        source venv/bin/activate
        pip install -r requirements.txt

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: 18

    - name: Install frontend dependencies
      working-directory: src/frontend
      run: npm install

    - name: Install mobile app dependencies
      working-directory: src/mobile_app
      run: npm install

    - name: Run frontend lint
      working-directory: src/frontend
      run: npm run lint

    - name: Run mobile app lint
      working-directory: src/mobile_app
      run: npm run lint

    - name: Build frontend
      working-directory: src/frontend
      run: npm run build

    - name: Check mobile app
      working-directory: src/mobile_app
      run: npm run build
